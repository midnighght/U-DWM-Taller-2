FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install --production

COPY . .

# seed on container start (attempt, then exit) - but we'll run seed via docker-compose command
# Expose port
EXPOSE 5000

CMD ["node", "server.js"]
