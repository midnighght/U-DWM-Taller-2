{
  "name": "com.webmovil.taller2",
  "displayName": "API Master",
  "version": "1.0.0",
  "description": "A sample Apache Cordova application that responds to the deviceready event.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "android:clean": "cordova clean android",
    "android:build:debug": "cordova build android --debug",
    "android:build:release": "cordova build android --release",
    "android:run:device": "cordova run android --device",
    "android:run:emulator": "cordova run android --emulator"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova": "^12.0.0",
    "cordova-android": "^14.0.1"
  },
  "cordova": {
    "platforms": [
      "android"
    ],
    "plugins": {}
  }
}